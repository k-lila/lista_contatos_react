class Contato {
  id: number
  nome: string
  fone: string
  email: string

  constructor(id: number, nome: string, fone: string, email: string) {
    this.id = id
    this.nome = nome
    this.fone = fone
    this.email = email
  }
}

export default Contato
